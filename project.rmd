---
title: "R Notebook"
output: html_notebook
---

# Final project

```{r message=FALSE}
require(BSDA)
library(BSDA)
require(EnvStats)
library(EnvStats)
library(dplyr)
```

### Sample data description

We decided to choose to analyze Steam PC game dataset.

```{r}
data = read.csv("games_data.csv")
data
```

### Hypothesis testing

#### 1. ...

```{r}
price_data <- as.vector(data$price) %>%
              lapply(function (value) as.double(gsub(",", "", value)))
price_data
mean(data$price)
#ks.test(price_data, "pnorm", mean(price_data), sd(price_data))
```

#### 2. ...

```{r}

```

### Conclusion

......
